# yaml-language-server: $schema=https://json.schemastore.org/taskfile.json
version: '3'
env:
  bash: bash --login -c --
  pwsh: powershell -command
  get : curl -Ls https://raw.githubusercontent.com/linitsh/scripts/main/remote
  node: node --input-type=module -e

tasks:
  default: echo "https://raw.githubusercontent.com/linitsh/scripts/main/shared.yml"

# includes:
#   shared: https://raw.githubusercontent.com/linitsh/scripts/main/shared.yml
